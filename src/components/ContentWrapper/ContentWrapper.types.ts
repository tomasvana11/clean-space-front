import { ReactNode } from "react";

export interface ContentWrapperProps {
  children: ReactNode;
  className?: string;
}

export interface ContentWrapperSmallProps {
  children: ReactNode;
  className?: string;
}
